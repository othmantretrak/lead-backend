services:
  app:
    build: . # ← Dockerfile is at root now, not ./app
    container_name: lead-gen-app
    restart: unless-stopped
    ports:
      - "3001:3001"
    env_file:
      - .env # ← loads your env vars automatically
    deploy:
      resources:
        limits:
          memory: 1g
